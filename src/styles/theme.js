import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    white: {
      main: '#FFFFFF'
    },
    cream: {
      main: '#FFCF53'
    },
    secondary: {
      main: '#757575',
      dark: '#00CC86'
    },
    primary: {
      main: '#212738',
      light: '#0275EB'
    }
  }
}) 

export default theme